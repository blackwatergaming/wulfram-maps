## Map contribution

Map slug(s):

What changed and why:

Playtesting performed:

## Checklist

- [ ] I changed only the intended map source and supporting documentation.
- [ ] I ran `npm test` and `npm run validate` from `wulfram-maps`, and `npm run maps:compile -- --all` from the sibling editor checkout.
- [ ] Every state gives both teams an uplink and a repair pad powered by a same-team power cell.
- [ ] I reviewed the text diff and did not add generated ZIP or `dist/` files.
- [ ] I am ready for an administrator to review this contribution before it reaches `main`.
